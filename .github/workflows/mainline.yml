name: Mainline workflow

on:
  check_run:
    types: [completed]

jobs:
  process_output:
    runs-on: ubuntu-latest
    if: github.event.check_run.conclusion == 'success' && github.event.check_run.name == 'build'
    
    steps:
      - name: Get workflow output
        run: |
          echo "The tag created in the Build workflow is: ${{ github.event.workflow_run.outputs.git_tag }}"